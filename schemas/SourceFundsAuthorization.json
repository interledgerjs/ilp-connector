{
  "$schema": "http://json-schema.org/draft-04/schema",
  "title": "FundsAuthorization",
  "description": "An authorization for the collection of source funds.",
  "type": "object",
  "allOf": [
    { "$ref": "Authorization.json" }
  ],
  "properties": {
    "sign_destination": {
      "description": "Whether or not the destination funds objects are retained when signing.",
      "type": { "enum": [ "all", "single", "none" ] },
      "default": "all"
    },
    "sign_sources": {
      "description": "Whether or not the source funds objects are retained when signing.",
      "type": { "enum": [ "all", "single" ] },
      "default": "all"
    }
  }
}
